// Const Thought requires Thought
const Thought = require('./Thought');
// Const User requires User
const User = require('./User');

// Exporting Thought and User
module.exports = { 
    Thought,
    User
 };